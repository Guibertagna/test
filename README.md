Number Manager 📱
A React project to manage phone numbers with features for adding, editing, deleting, and pagination.

How to Run the Project
1. Install dependencies
Run the following command to install all dependencies:


Copiar código
npm install
2. Start the mock backend
Make sure you have json-server installed globally. To start the server, run:


Copiar código
json-server --watch db.json --port 5000
The db.json file is included in the project and contains the initial data.

3. Start the React application
To start the application, run:


Copiar código
npm start
The application will be available at: http://localhost:3000.

Technologies Used
React
Bootstrap
json-server
